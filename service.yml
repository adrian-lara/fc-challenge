kind: Service
apiVersion: v1
metadata:
  name: fc-challenge-service
spec:
  selector:
    app: fc-challenge
  ports:
    - protocol: "TCP"
      port: 8080
      targetPort: 3000
      nodePort: 30001
  type: LoadBalancer
